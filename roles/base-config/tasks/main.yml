---
- name: enable sudo without tty for some ansible commands
  replace:
    path: /etc/sudoers
    regexp: '^Defaults\s*requiretty'
    replace: 'Defaults  !requiretty'
    backup: yes

- name: Enable repos
  template:
    src: repos_template.j2
    dest: /etc/yum.repos.d/tier-three-apps.repo
    owner: root
    group: root
    mode: 0644

